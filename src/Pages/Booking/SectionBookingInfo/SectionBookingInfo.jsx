/* import packages */
import clsx from "clsx";
import React from "react";

export default function SectionBookingInfo({ className, bookingInfo }) {
  return (
    <div className={clsx("section-booking-info", className)}>
      SectionBookingInfo
    </div>
  );
}
